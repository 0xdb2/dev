prompt ##### start sql

set verify off;
set head off
set feedback off
--set sqlterminator ';'

select systimestamp from dual;

define dry_run=true;

@@declare

declare

    sql_stmt VARCHAR(32000);

    &def_decl

begin

    drop_immediate('view tmp_emp_cols');

    putline('################## pl-sql block');

    sql_stmt := '
    create view tmp_emp_cols as
        select * from emp
        -----------------------------------------------------------------------------------------------
        -----------------------------------------------------------------------------------------------
        -----------------------------------------------------------------------------------------------
        -----------------------------------------------------------------------------------------------
        -----------------------------------------------------------------------------------------------
        -----------------------------------------------------------------------------------------------
        -----------------------------------------------------------------------------------------------
        -----------------------------------------------------------------------------------------------
        -----------------------------------------------------------------------------------------------
        -----------------------------------------------------------------------------------------------
        -----------------------------------------------------------------------------------------------
        -----------------------------------------------------------------------------------------------
        -----------------------------------------------------------------------------------------------
    ';
    exec_immediate(sql_stmt);

    sql_stmt := '
    drop view tmp_emp_cols
    ';
    exec_immediate(sql_stmt);

exception when others then
    putline('exception: ' || sqlerrm(sqlcode));
end;
/

exit 0;
